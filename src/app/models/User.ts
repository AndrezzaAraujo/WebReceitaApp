import { BrowserDynamicTestingModule } from "@angular/platform-browser-dynamic/testing";

export interface User {
    confirmPassword: any;
    id: number;
    password: string;
    email: string;
    DateBirth: BrowserDynamicTestingModule;
    isUser: boolean;
    isLogin: boolean;
}
